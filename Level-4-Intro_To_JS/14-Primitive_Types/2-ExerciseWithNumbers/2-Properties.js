// Epsilon - smallest interval between two numbers

const age = new Number(36);
typeof age; //object
age.valueOf(); //36

console.log(Number.EPSILON); // Smallest interval between two numbers
console.log(Number.MAX_SAFE_INTEGER); // Max positive value JS can represent
console.log(Number.MAX_VALUE); // maximum positive value JavaScript can represent
console.log(Number.MIN_SAFE_INTEGER); // the minimum integer value JavaScript can represent
console.log(Number.MIN_VALUE); // the minimum positive value JavaScript can represent
console.log(Number.NaN); // special value representing “not a number”
console.log(Number.NEGATIVE_INFINITY); // special value representing negative infinity
console.log(Number.POSITIVE_INFINITY); // special value representing positive infinity
